# HTTP/2 + 连接池优化依赖
# 第一阶段：网络层升级依赖

# HTTP/2 支持
httpx[http2]>=0.24.0
h2>=4.1.0
hpack>=4.0.0

# 向后兼容 (保留原有aiohttp)
aiohttp>=3.8.0
aiofiles>=23.1.0

# 性能监控和调试
psutil>=5.9.0  # 系统性能监控

# 异步支持
asyncio-throttle>=1.0.2  # 流量控制

# 已有依赖 (保持兼容)
PySide6>=6.4.0 